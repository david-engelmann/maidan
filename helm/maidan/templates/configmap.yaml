apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "maidan.fullname" . }}-config
  labels:
    {{- include "maidan.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
