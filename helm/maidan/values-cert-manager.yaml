# Production ingress with cert-manager (Cluster 55).
# Requires cert-manager + a ClusterIssuer named below.
replicaCount: 2

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "32m"
  hosts:
    - host: maidan.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: maidan-tls
      hosts:
        - maidan.example.com

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 20
  targetCPUUtilizationPercentage: 65

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "2"
    memory: 1Gi

persistence:
  enabled: true
  size: 50Gi
