# Overlay: multi-replica rate limits + per-token quotas via Redis.
# Point at your Redis/Valkey service; omit to use in-memory limits (single replica).
secrets:
  MAIDAN_RATE_LIMIT_REDIS_URL: "redis://redis:6379"
