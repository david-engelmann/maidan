# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# sqlx offline cache (committed selectively; ignore drafts)
.sqlx/.tmp/

# Build + dist
/dist/
/build/

# mdBook (generated): build output + docs staged in by book/sync-docs.sh
/book/build/
/book/src/docs/

# Python (examples/ client recipes — bytecode + venvs are never committed)
__pycache__/
*.pyc
.venv/

# Coverage
*.profraw
*.profdata
/coverage/
lcov.info
tarpaulin-report.html

# Test artifacts
**/proptest-regressions/
**/snapshots/*.snap.new
/tests/load/results/
/tests/red-team/results/

# Local env + secrets (committed example only)
.env
.env.*
!.env.example
maidan.toml
!maidan.toml.example
*.pem
*.key
*.crt

# Local DBs
*.db
*.db-journal
!docker/Dockerfile.db
*.sqlite
*.sqlite-journal

# Local object storage
/.local/
/data/

# Logs
*.log
/logs/

# Docker — local compose overrides (auto-merged by `docker compose`; per-machine,
# may hold dev secrets/ports). Both the legacy and modern Compose file names.
docker-compose.override.yml
docker-compose.override.yaml
compose.override.yml
compose.override.yaml

# IDE / editor
.idea/
.vscode/*
!.vscode/settings.json.example
*.swp
*.swo
*~
.nvim.lua

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Linux
*~
.directory

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Direnv
.envrc.local

# Raw rules extracted from the PrometheusRule CRD by scripts/check-alert-rules.sh
docs/alerts/slo-rules.generated.yaml
