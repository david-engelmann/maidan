# Cluster 34.0 — MCP streamable session

**Theme:** `Mcp-Session-Id` header on `POST /mcp/streamable` for client correlation.

## Tag

`v34.0.0`

## Out of scope

Full bidirectional JSON-RPC mux on one HTTP connection (spec-complete streamable transport).
