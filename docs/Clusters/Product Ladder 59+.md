# Product Ladder 59+ — Agent substrate hardening

**North star:** Make Maidan the most reliable agent integration surface — stable contracts, complete transports, trustworthy delivery, and rich context export.

**Predecessor:** [[Clusters/Product Ladder 35+]] closed at **`maidan-2.0`**.

**Versioning:** One cluster → one tag (`v59.0.0` …).

---

## Phase VIII — Agent contract

| Cluster | Theme | Tag | Exit |
|---------|--------|-----|------|
| **59** | Agent integration charter | `v59.0.0` | [[Agent Integration]] + contract golden files in CI |

## Phase IX — Transport & trust

| Cluster | Theme | Tag | Exit |
|---------|--------|-----|------|
| **60** | MCP streamable sessions | `v60.0.0` | Session TTL + `DELETE /mcp/streamable` |
| **61** | A2A external runtime | `v61.0.0` | Agent card + push notification config RPC |
| **62** | Subscribe & resume v2 | `v62.0.0` | `schema_version` on subscribe ack + quarantined outbox list |
| **63** | Capabilities matrix | `v63.0.0` | MCP tool denial coverage in capability e2e |
| **64** | MCP quotas | `v64.0.0` | Per-token quotas on `tools/call` |

## Phase X — Onboarding & context

| Cluster | Theme | Tag | Exit |
|---------|--------|-----|------|
| **65** | App install OAuth | `v65.0.0` | Authorization code + token exchange |
| **66** | Discovery | `v66.0.0` | `/.well-known/maidan.json` includes MCP + agent card |
| **67** | Context packages | `v67.0.0` | Workspace context + message edits in thread context |

---

## Continuation

Ladder **68+** closed at **`v76.0.0`** / **`maidan-agent-1.0`**. Next: [[Clusters/Product Ladder 77+]].
