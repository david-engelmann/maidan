# Cluster 32.0 — Helm umbrella

**Theme:** `helm/maidan-stack` wraps `maidan` with optional Bitnami Postgres and MinIO.

## Tag

`v32.0.0`

## CI

`scripts/helm-template-smoke.sh` templates maidan-stack when `Chart.lock` is present.
