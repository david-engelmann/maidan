# Cluster 100.0 retro — mcp-stdio embedded

> Tag **`v100.0.0`**.

## What shipped

- `maidan mcp-stdio` spawns in-process bus + indexer (`LoggingHandler` / `EmbeddingHandler`).
- `McpServer::with_event_bus` publishes `MessagePosted` from MCP writes for demo indexing.

## Next

Cluster **101** — operator product gate ([[Clusters/Product Ladder 77+]]).
