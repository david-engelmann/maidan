# Cluster 70.0 retro — Vault truth pass

> Tag **`v70.0.0`**.

## What shipped

- [[Architecture]] — `v69` baseline, agent substrate snapshot, current API table, accurate deferrals.
- [[Remaining Work]] — reconciled partials (pins, slash, erase, Helm, capabilities, outbox replay).
- [[Open Work]] — latest tag **`v69`**, ladder **68–70** closes, pointer to **71**.
- Root `README.md` — agent integration pitch; trimmed stale v28-only status.
- [[README]] vault index — ladder **68+** Phase XI close; active **71**.

## What was deferred

- mdBook page-by-page rewrite (vault is source of truth).
- Per-retro files for clusters 23–27 (history lives in [[CHANGELOG]]).
- Cluster **71** implementation (event contract v2).

## Forward look

Cluster **71**: Event & subscribe contract v2 ([[Clusters/Product Ladder 68+]] Phase XII).
