# Cluster 98.0 retro — Mention notifications

> Tag **`v98.0.0`**.

## What shipped

- `workspaces.mention_webhook_id`; `GET/PUT /workspaces/:wid/mention-webhook`.
- Webhook worker routes `mention_recorded` to configured subscription.

## Next

Cluster **99** — presence v2 docs ([[Clusters/Product Ladder 77+]]).
