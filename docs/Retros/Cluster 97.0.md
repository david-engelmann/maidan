# Cluster 97.0 retro — Group DMs

> Tag **`v97.0.0`**.

## What shipped

- `maidan_group_dm_*` schema; `open_group_dm_conversation` (≥3 members).
- HTTP routes + `group_dm_e2e` + store tests.

## Next

Cluster **98** — mention webhooks ([[Clusters/Product Ladder 77+]]).
