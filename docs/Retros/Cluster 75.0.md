# Cluster 75.0 retro — Semantic scale

> Tag **`v75.0.0`**.

## What shipped

- Production runbook: Helm should set a real embedding provider; `maidan reindex-embeddings` documented (CLI already shipped).
- Score semantics cross-backend documented in [[Production]] / [[Architecture]].

## Forward look

Cluster **76** agent observability gate.
