# Cluster 93.0 retro — /ui live events

> Tag **`v93.0.0`**.

## What shipped

- WS subscribe presets (workspace / channel / thread / messages+mentions), auto-reconnect, `resume_token` in localStorage.
- Session cookie auth on `/ws/subscribe` (no bearer when OIDC session present).
- `ui_ws_tail_e2e`; UI `data-ui-version="7"`.

## Next

Cluster **94** — artifacts in thread view ([[Clusters/Product Ladder 77+]]).
