# Cluster 74.0 retro — Context export parity

> Tag **`v74.0.0`**.

## What shipped

- MCP tools `get_thread_context` and `get_workspace_context` with bounded limits.
- Contract catalog + capability map updated (30 tools).

## What was deferred

- Cursor-based pagination across very large transcripts (limits only).

## Forward look

Cluster **75** semantic scale / reindex ops.
