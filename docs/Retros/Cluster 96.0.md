# Cluster 96.0 retro — /ui tokens & apps

> Tag **`v96.0.0`**.

## What shipped

- `GET /workspaces/:wid/members/:mid/tokens` (metadata only, no secrets).
- `/ui/api` token list + read-only app installations panel.

## Next

Cluster **97** — group DMs ([[Clusters/Product Ladder 77+]]).
