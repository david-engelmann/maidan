apiVersion: v1
kind: Service
metadata:
  name: maidan-server
  namespace: maidan
  labels:
    app.kubernetes.io/name: maidan
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: maidan
    app.kubernetes.io/component: server
