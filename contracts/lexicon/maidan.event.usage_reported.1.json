{
  "$id": "maidan.event.usage_reported/1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "Wire envelope for `usage_reported`. `$type` is the contract: new fields optional, no renames, unknown ignored, breaking = new type.",
  "properties": {
    "$type": {
      "const": "maidan.event.usage_reported/1",
      "type": "string"
    },
    "kind": {
      "const": "usage_reported",
      "type": "string"
    }
  },
  "required": [
    "$type",
    "kind"
  ],
  "title": "maidan.event.usage_reported/1",
  "type": "object"
}
