{
  "$id": "maidan.event.approval_requested/1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "Wire envelope for `approval_requested`. `$type` is the contract: new fields optional, no renames, unknown ignored, breaking = new type.",
  "properties": {
    "$type": {
      "const": "maidan.event.approval_requested/1",
      "type": "string"
    },
    "kind": {
      "const": "approval_requested",
      "type": "string"
    }
  },
  "required": [
    "$type",
    "kind"
  ],
  "title": "maidan.event.approval_requested/1",
  "type": "object"
}
