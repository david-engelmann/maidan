{
  "$id": "maidan.event.wait_timed_out/1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "Wire envelope for `wait_timed_out`. `$type` is the contract: new fields optional, no renames, unknown ignored, breaking = new type.",
  "properties": {
    "$type": {
      "const": "maidan.event.wait_timed_out/1",
      "type": "string"
    },
    "kind": {
      "const": "wait_timed_out",
      "type": "string"
    }
  },
  "required": [
    "$type",
    "kind"
  ],
  "title": "maidan.event.wait_timed_out/1",
  "type": "object"
}
